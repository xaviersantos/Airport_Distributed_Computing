/**
 * Group of the intervening entities in the problem: Passenger, Porter,
 * Bus Driver and his states.
 * Each one of them is a separate class. Each entity has a life-cycle, and an
 * internal state that will update itself over time.
 * The states are also separated classes.
 */
package airport.InterveningEntities;
